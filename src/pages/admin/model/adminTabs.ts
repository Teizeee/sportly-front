import type { TabKey } from './types'

export const adminTabs: Array<{ key: TabKey; label: string }> = [
  { key: 'applications', label: 'Заявки' },
  { key: 'editing', label: 'Редактирование' },
  { key: 'reviews', label: 'Отзывы' },
  { key: 'subscriptions', label: 'Подписки' },
]

